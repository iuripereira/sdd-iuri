# Tasks — delta-009
<!-- ordenado por dependência; cada task executável sem contexto extra -->
- [ ] T1 — `PR_LIMITE = 500` no topo do `check_cycle.py` + `check_cycle.py` nos mirrors do bloco `pr-limiar-tamanho` do `deps.toml` (comentário: 2 espelhos) · arquivos: skills/spec-feature/scripts/check_cycle.py, deps.toml · cobre: R1 · verificação: `python3 skills/guarding-doc-integrity/scripts/validate_integrity.py` PASS (C1 dono + 2 espelhos)
- [ ] T2 (dep: T1) — `selftest_c7()` (git real, molde do `selftest_c4`): artefato > limiar acusa BAIXO; artefato pequeno passa limpo; chamado ao fim de `selftest()` · arquivos: skills/spec-feature/scripts/check_cycle.py · cobre: R1 · verificação: `check_cycle.py --selftest` falha (RED — função ausente)
- [ ] T3 (dep: T2) — `c7_split(root, delta, v)`: `base_c4` + `git diff <base> --numstat -- <delta-rel>`, soma inserções, BAIXO acima de `PR_LIMITE`; omite sem git/base; chamado em `checar()` após C6 · arquivos: skills/spec-feature/scripts/check_cycle.py · cobre: R1 · verificação: `check_cycle.py --selftest` PASS (GREEN); `check_cycle.py specs/009-split-pr-mecanico` roda sem quebrar
- [ ] T4 (dep: T3) — atualizar consumidores: docstring do `check_cycle.py` (C1–C7), `analyze.md` linha do split (C7 mede), `cycle.md` se citar C1–C6 · arquivos: skills/spec-feature/scripts/check_cycle.py, skills/spec-feature/references/analyze.md, skills/spec-feature/references/cycle.md · cobre: infra · verificação: `grep -rn 'C1–C6\|C1-C6\|C6)' skills/spec-feature` sem menção defasada
- [ ] T5 (dep: T4) — CHANGELOG `[Não lançado]` (Adicionado: C7) + STATE (Agora/Feito) · arquivos: CHANGELOG.md, STATE.md · cobre: infra · verificação: leitura — entrada presente e coerente
