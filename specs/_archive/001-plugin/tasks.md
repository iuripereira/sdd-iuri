# Tasks — Δ 001
<!-- ordenado por dependência; cada task executável sem contexto extra -->
<!-- detalhe passo a passo em plan.md; aqui é o mapa task → requisito → verificação -->

- [x] T1 — criar clone de trabalho fora de `~/.claude/skills` (mover as skills in-place quebraria a descoberta) · arquivos: `~/dev/sdd-iuri/` (clone novo) · cobre: infra · verificação: `ls ~/dev/sdd-iuri` lista os 5 diretórios de skill e nenhuma das 12 skills pessoais; `ls ~/.claude/skills/spec-feature/SKILL.md` segue existindo
- [x] T2 (dep: T1) — mover as 5 skills para `skills/` e criar o manifesto do plugin · arquivos: `skills/{projeto-init,projeto-infra,spec-feature,spec-review,guarding-doc-integrity}/`, `.claude-plugin/plugin.json`, `.github/workflows/ci.yml` · cobre: R1 · verificação: `git status --short` mostra renames (`R `); `python3 -m json.tool .claude-plugin/plugin.json`; os dois `--selftest` passam dos caminhos novos
- [x] T3 (dep: T2) — validar `${CLAUDE_PLUGIN_ROOT}` e o namespace em execução, antes de qualquer passo depender deles · arquivos: nenhum versionado (instalação de teste a partir da branch) · cobre: RNF1 · verificação: as 5 skills aparecem como `sdd-iuri:<nome>`; invocar `sdd-iuri:guarding-doc-integrity` imprime caminho absoluto real, não o literal `${CLAUDE_PLUGIN_ROOT}` — se sair o literal, PARAR e reescrever a métrica do RNF1
- [x] T4 (dep: T3) — trocar os 4 caminhos absolutos de máquina por `${CLAUDE_PLUGIN_ROOT}` e adicionar o step de CI que o verifica · arquivos: `skills/spec-feature/references/analyze.md`, `skills/spec-feature/references/adapters.md`, `skills/guarding-doc-integrity/SKILL.md`, `skills/projeto-init/references/detection.md`, `.github/workflows/ci.yml` · cobre: RNF1 · verificação: o grep falha antes da correção e `grep -rn '~/.claude/skills' skills/ .github/` sai vazio depois
- [x] T5 (dep: T3) — prefixar as 33 citações de comando com o namespace confirmado em T3 · arquivos: `skills/*/SKILL.md`, `skills/projeto-init/references/{canonical-rules,detection}.md`, `skills/spec-feature/references/analyze.md`, `README.md`, `CLAUDE.md`, `specs/TRUTH.md` · cobre: R2, R3, R4, R5, R6 · verificação: zero citação sem namespace fora de `_archive/` e `CHANGELOG.md`; nenhum falso positivo do sed em caminho de arquivo; `check_cycle.py --selftest` segue passando
- [x] T6 (dep: T2) — converter o `.gitignore` de allowlist para denylist, só dentro do clone · arquivos: `.gitignore` · cobre: infra · verificação: `pwd` termina em `dev/sdd-iuri` (guarda obrigatória); `git status --short` mostra apenas `M .gitignore` e nenhum arquivo que a allowlist escondia
- [x] T7 (dep: T4, T5, T6) — reescrever a instalação e remover do `CLAUDE.md`/`STATE.md` o que a allowlist justificava; registrar no changelog; abrir o PR · arquivos: `README.md`, `CLAUDE.md`, `STATE.md`, `CHANGELOG.md` · cobre: R1 · verificação: `[Não lançado]` do CHANGELOG cita a quebra de invocação; suíte completa verde (JSON, YAML, grep do RNF1, dois selftests, `check_cycle.py specs/001-plugin`); checks `ci` e `commits` verdes no PR
- [ ] T8 (dep: T7 mergeado) — renomear o repo, reinstalar do `main` e só então apagar o estado antigo · arquivos: `~/.claude/skills/{5 skills,docs,specs,.github,.git,CLAUDE.md,CHANGELOG.md,STATE.md,README.md,.gitignore}` (remoção) · cobre: R1 · verificação: as 5 skills respondem por `sdd-iuri:` **antes** de qualquer remoção; clone sincronizado com `origin/main`; ao fim `ls ~/.claude/skills | wc -l` = 12 e `sdd-iuri:spec-feature` invocável
